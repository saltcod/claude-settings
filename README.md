Some claude code settings for new projects so you don't have to keep approving common actions.

1. run `claude` from your project directory
2. run `/init` to create a .claude folder
3. replace `.claude/settings.local.json` with this file
